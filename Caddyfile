:8080 {
    # RPC endpoint
    handle /rpc {
        reverse_proxy localhost:8899
    }

    # WebSocket endpoint
    handle /ws {
        reverse_proxy localhost:8900
    }

    # Studio UI at root (SPA-friendly)
    handle /* {
        reverse_proxy localhost:18488
    }
}
